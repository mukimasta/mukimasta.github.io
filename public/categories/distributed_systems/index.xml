<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" >
  <channel>
    <title>Distributed_systems on MUKIMASTA</title>
    <link>https://mukimasta.github.io/categories/distributed_systems/</link>
    <description>Recent Posts in Distributed_systems on MUKIMASTA</description>
    <generator>Hugo 0.151.0</generator>
    <language>en-GB</language>
    <managingEditor>mukimasta@outlook.com (Hanlin Xue)</managingEditor>
    <webMaster>mukimasta@outlook.com (Hanlin Xue)</webMaster>
    <lastBuildDate>Mon, 02 Mar 2026 13:53:19 -0800</lastBuildDate>
    <atom:link href="https://mukimasta.github.io/categories/distributed_systems/index.xml" rel="self" type="application/rss+xml" />
        <atom:link href="https://mukimasta.github.io/zh/categories/distributed_systems/index.xml" hreflang="zh" rel="alternate" type="application/rss&#43;xml" />
        
    <item>
      <title>Distributed System Blog 01 – Exactly-Once RPC</title>
      <link>https://mukimasta.github.io/distributed_systems/blog1/</link>
      <pubDate>Tue, 20 Jan 2026 00:00:00 +0000</pubDate>
      <author>mukimasta@outlook.com (Hanlin Xue)</author>
      <dc:creator>[Mukii]</dc:creator>
      <guid>https://mukimasta.github.io/distributed_systems/blog1/</guid>
      <description>Implementing Exactly-Once RPC over unreliable networks</description>
      <content:encoded>&lt;h1 id=&#34;distributed-system-blog-01--exactly-once-rpc&#34;&gt;Distributed System Blog 01 – Exactly-Once RPC&lt;/h1&gt;&#xA;&lt;p&gt;&lt;strong&gt;Implementing Exactly-Once RPC over unreliable networks&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;h2 id=&#34;0--preface&#34;&gt;0  Preface&lt;/h2&gt;&#xA;&lt;h3 id=&#34;01--distributed-systems&#34;&gt;0.1  Distributed Systems&lt;/h3&gt;&#xA;&lt;p&gt;We use distributed systems every day:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Open WeChat → your phone connects to Tencent&amp;rsquo;s servers&lt;/li&gt;&#xA;&lt;li&gt;Scroll TikTok → videos stream from ByteDance&amp;rsquo;s servers to you&lt;/li&gt;&#xA;&lt;li&gt;Shop online → order data lives in Alibaba&amp;rsquo;s databases&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;p&gt;&lt;strong&gt;Distributed system = multiple independent computing nodes collaborating over a network to achieve a common goal&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;p&gt;&lt;strong&gt;Why do we need distributed systems?&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Single machine can&amp;rsquo;t hold it all: Facebook has 3 billion users; one machine can&amp;rsquo;t store all that data&lt;/li&gt;&#xA;&lt;li&gt;Single machine can&amp;rsquo;t handle the load: Black Friday sees hundreds of thousands of orders per second; one machine can&amp;rsquo;t process that&lt;/li&gt;&#xA;&lt;li&gt;Single machines fail: servers break, disks burn out, we need backups&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;p&gt;But multiple machines bring new problems: how do they communicate and collaborate over the network?&lt;/p&gt;&#xA;&lt;h3 id=&#34;02--remote-procedure-call-rpc&#34;&gt;0.2  Remote Procedure Call (RPC)&lt;/h3&gt;&#xA;&lt;p&gt;Think about how you use apps:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Tap a friend&amp;#39;s avatar → see their profile&#xA;Refresh feed → see latest updates&#xA;Send a message → recipient gets it&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Behind the scenes, these operations are like calling a remote function.&lt;/p&gt;&#xA;&lt;p&gt;Imagine you&amp;rsquo;re writing code:&lt;/p&gt;&#xA;&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-python&#34; data-lang=&#34;python&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;c1&#34;&gt;# Local function call&lt;/span&gt;&#xA;&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;n&#34;&gt;profile&lt;/span&gt; &lt;span class=&#34;o&#34;&gt;=&lt;/span&gt; &lt;span class=&#34;n&#34;&gt;get_profile&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;(&lt;/span&gt;&lt;span class=&#34;n&#34;&gt;user_id&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;)&lt;/span&gt;&#xA;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;This function runs in your program and reads data from local memory.&lt;/p&gt;&#xA;&lt;p&gt;But what if the data lives on another machine?&lt;/p&gt;&#xA;&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-python&#34; data-lang=&#34;python&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;c1&#34;&gt;# Remote function call (RPC)&lt;/span&gt;&#xA;&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;n&#34;&gt;profile&lt;/span&gt; &lt;span class=&#34;o&#34;&gt;=&lt;/span&gt; &lt;span class=&#34;n&#34;&gt;remote_get_profile&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;(&lt;/span&gt;&lt;span class=&#34;n&#34;&gt;user_id&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;)&lt;/span&gt;&#xA;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;This function:&lt;/p&gt;&#xA;&lt;ol&gt;&#xA;&lt;li&gt;Sends the request over the network to the server&lt;/li&gt;&#xA;&lt;li&gt;The server executes the function&lt;/li&gt;&#xA;&lt;li&gt;Sends the result back over the network&lt;/li&gt;&#xA;&lt;/ol&gt;&#xA;&lt;p&gt;It looks like we just added &amp;ldquo;remote,&amp;rdquo; but once the network is involved, everything changes.&lt;/p&gt;&#xA;&lt;h3 id=&#34;03--a-transfer-story&#34;&gt;0.3  A Transfer Story&lt;/h3&gt;&#xA;&lt;p&gt;Imagine this scenario:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;You open the app, transfer $100 to a friend&#xA;Tap &amp;#34;Confirm Transfer&amp;#34;&#xA;loading...&#xA;loading...&#xA;10 seconds pass, still loading&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;What do you do?&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Tap again? What if the first one succeeded? Then you&amp;rsquo;d transfer $200&lt;/li&gt;&#xA;&lt;li&gt;Give up? What if it never went through?&lt;/li&gt;&#xA;&lt;li&gt;Wait? Wait for how long?&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;p&gt;&lt;strong&gt;This is the core dilemma of distributed systems: the network is unreliable&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;h3 id=&#34;04--how-unreliable-is-the-network&#34;&gt;0.4  How Unreliable Is the Network?&lt;/h3&gt;&#xA;&lt;p&gt;You sent &amp;ldquo;transfer $100&amp;rdquo; and got no reply. What happened?&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Case 1: The request was lost in transit&#xA;Client ----X---&amp;gt; Server&#xA;(Server never received it)&#xA;&#xA;Case 2: Server executed it, but the reply was lost&#xA;Client --------&amp;gt; Server ✔️&#xA;Client &amp;lt;---X---- Server&#xA;(Money transferred, but you don&amp;#39;t know)&#xA;&#xA;Case 3: The network is slow&#xA;Client ----😴---&amp;gt; Server 🕒&#xA;(Still on the way, looks like it was lost)&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Key dilemma: You cannot distinguish these cases, nor can you guarantee the order in which messages arrive.&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;h3 id=&#34;05--our-goal-exactly-once-rpc&#34;&gt;0.5  Our Goal: Exactly-Once RPC&lt;/h3&gt;&#xA;&lt;p&gt;Regardless of network conditions, we want to guarantee: each operation is executed exactly once (Exactly-Once).&lt;/p&gt;&#xA;&lt;p&gt;How do we achieve &amp;ldquo;Exactly-Once&amp;rdquo;?&lt;/p&gt;&#xA;&lt;p&gt;&lt;strong&gt;Exactly-Once = At-Least-Once + At-Most-Once&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;p&gt;Breaking it down:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;&lt;strong&gt;Cannot be 0 times&lt;/strong&gt; → execute at least 1 time (At-Least-Once)&lt;/li&gt;&#xA;&lt;li&gt;&lt;strong&gt;Cannot be 2+ times&lt;/strong&gt; → execute at most 1 time (At-Most-Once)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h2 id=&#34;1--at-least-once-rpc&#34;&gt;1  At-Least-Once RPC&lt;/h2&gt;&#xA;&lt;p&gt;Let&amp;rsquo;s implement At-Least-Once RPC first—ensuring each operation is executed by the Server at least once, and the Client always receives the result.&lt;/p&gt;&#xA;&lt;p&gt;At-Least-Once is implemented at the Client layer.&lt;/p&gt;&#xA;&lt;h3 id=&#34;11--retry-mechanism&#34;&gt;1.1  Retry Mechanism&lt;/h3&gt;&#xA;&lt;p&gt;First, let&amp;rsquo;s solve the case where the request may be lost—i.e., the Server never received it:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;  Client    ----X---&amp;gt;    Server&#xA;Transfer $100  (packet loss)  (never received)&#xA;&#xA;Result: 0 executions&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;What to do?&lt;/p&gt;&#xA;&lt;p&gt;An intuitive idea: retry.&lt;/p&gt;&#xA;&lt;p&gt;We resend the request at intervals until we successfully receive a reply.&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;  Client    ----X---&amp;gt;    Server&#xA;Transfer $100  (packet loss)  (never received)&#xA;&#xA;  Client    --------&amp;gt;    Server&#xA;Transfer $100   (retry)   (executed successfully)&#xA;&#xA;  Client    &amp;lt;--------    Server&#xA; (got reply)             (reply with result)&#xA;&#xA;Result: 1 execution&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Seems reasonable, but this creates a problem.&lt;/p&gt;&#xA;&lt;p&gt;Consider this scenario:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;T1: Client sends &amp;#34;transfer $100&amp;#34;&#xA;T2: (timeout, no reply)&#xA;T3: Client retries, sends &amp;#34;transfer $100&amp;#34; again&#xA;T4: Server replies &amp;#34;success&amp;#34;&#xA;T5: Client sends new command &amp;#34;query balance&amp;#34;&#xA;T6: Server replies &amp;#34;success&amp;#34;&#xA;&#xA;Question: Which command is this &amp;#34;success&amp;#34; a reply to?&#xA;- The transfer?&#xA;- The balance query?&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;If the Client mistakenly thinks &amp;ldquo;success&amp;rdquo; is the reply to &amp;ldquo;query balance&amp;rdquo; when it&amp;rsquo;s actually the delayed reply from the first &amp;ldquo;transfer,&amp;rdquo; the system is in chaos.&lt;/p&gt;&#xA;&lt;p&gt;&lt;strong&gt;Problem essence: replies and requests have lost their correspondence.&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;h3 id=&#34;12--requestid-for-request-identification&#34;&gt;1.2  RequestID for Request Identification&lt;/h3&gt;&#xA;&lt;p&gt;How do we establish correspondence? Tag each request.&lt;/p&gt;&#xA;&lt;p&gt;The tag must satisfy:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Stay the same on retry (so we know it&amp;rsquo;s the same request)&lt;/li&gt;&#xA;&lt;li&gt;Change for new commands (so we can distinguish different requests)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;p&gt;&lt;strong&gt;Natural choice: monotonically increasing request_id&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Client maintains a counter:&#xA;request_id = 0&#xA;&#xA;Send new command:&#xA;  request_id += 1&#xA;  send (request_id, command)&#xA;&#xA;On retry:&#xA;  send the same (request_id, command)&#xA;  (request_id unchanged!)&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;The Server&amp;rsquo;s reply must also include request_id, otherwise the Client cannot match them.&lt;/p&gt;&#xA;&lt;p&gt;Concrete scenario:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;T1: Client sends (request_id=1, &amp;#34;transfer $100&amp;#34;)&#xA;T2: (timeout, about to retry) &#xA;T3: Client sends (request_id=1, &amp;#34;transfer $100&amp;#34;)  ← ID unchanged&#xA;T4: Server replies (request_id=1, &amp;#34;success&amp;#34;)&#xA;T5: Client sends (request_id=2, &amp;#34;query balance&amp;#34;) ← ID incremented&#xA;T6: Server replies (request_id=1, &amp;#34;success&amp;#34;)&#xA;&#xA;Client checks: request_id=1 ≠ current request_id=2&#xA;→ This is an old reply, ignore it&#xA;→ Keep waiting for reply with request_id=2&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Complete message format:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Request:&#xA;  request_id: which request (sequence number)&#xA;  command:    what to execute&#xA;&#xA;Reply:&#xA;  request_id: which request this replies to&#xA;  result:     execution result&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;13--complete-at-least-once-logic&#34;&gt;1.3  Complete At-Least-Once Logic:&lt;/h3&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Client state:&#xA;- current_request_id: ID of current request&#xA;- current_command:    command of current request&#xA;- timer:              retry timer&#xA;&#xA;Send new command:&#xA;  current_request_id += 1&#xA;  current_command = command&#xA;  send Request(current_request_id, command)&#xA;  start timer&#xA;&#xA;On receiving Reply(reply_request_id, result):&#xA;  if reply_request_id == current_request_id:&#xA;      stop timer&#xA;      return result&#xA;  else:&#xA;      ignore (this is a reply to an old request)&#xA;&#xA;On timer timeout:&#xA;  resend Request(current_request_id, current_command)&#xA;  restart timer&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Thus we achieve At-Least-Once: as long as the Client keeps retrying, the request will eventually reach and execute, and the Client will eventually receive the reply.&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;h2 id=&#34;2--at-most-once-rpc&#34;&gt;2  At-Most-Once RPC&lt;/h2&gt;&#xA;&lt;p&gt;Client retries cause the Server to receive multiple identical requests. At-Most-Once must ensure each request is executed only once.&lt;/p&gt;&#xA;&lt;p&gt;At-Most-Once is implemented at the Server layer.&lt;/p&gt;&#xA;&lt;h3 id=&#34;21--deduplication-mechanism&#34;&gt;2.1  Deduplication Mechanism&lt;/h3&gt;&#xA;&lt;p&gt;How does the Server know it&amp;rsquo;s a duplicate request?&lt;/p&gt;&#xA;&lt;p&gt;Use what we already have: &lt;strong&gt;request_id&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Server maintains a record:&#xA;seen = {1, 2, 5, 7, ...}&#xA;&#xA;On receiving Request(request_id, command):&#xA;  if request_id in seen:&#xA;      this is a duplicate request&#xA;      ...&#xA;  else:&#xA;      this is a new request&#xA;      ...&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;For multiple Clients:&lt;/p&gt;&#xA;&lt;p&gt;The Server&amp;rsquo;s record key becomes (client_id, request_id):&lt;/p&gt;&#xA;&lt;p&gt;In network communication, every message naturally carries the sender&amp;rsquo;s address. We use this address as client_id.&lt;/p&gt;&#xA;&lt;p&gt;So the complete unique identifier for a request is: (client_id, request_id), which is what the Server layer maintains.&#xA;Where:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;&lt;strong&gt;client_id&lt;/strong&gt;: provided by the network framework (sender&amp;rsquo;s address)&lt;/li&gt;&#xA;&lt;li&gt;&lt;strong&gt;request_id&lt;/strong&gt;: maintained by our application layer (monotonically increasing sequence number)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h3 id=&#34;22--request--result-mapping-table&#34;&gt;2.2  Request → Result Mapping Table&lt;/h3&gt;&#xA;&lt;p&gt;What to do when receiving a duplicate request?&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;T1: Client sends Request(request_id=1, command)&#xA;    Server executes, reply is lost&#xA;&#xA;T2: Client retries, sends Request(request_id=1, command)&#xA;    Server finds: seen (client_id, 1) before&#xA;    Then what?&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Server cannot:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Re-execute → violates at-most-once&lt;/li&gt;&#xA;&lt;li&gt;Not reply → Client will wait forever&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;p&gt;&lt;strong&gt;Server must: return the previous result&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;p&gt;For each request (client_id, request_id), save the corresponding result.&lt;/p&gt;&#xA;&lt;p&gt;&lt;strong&gt;HashMap: (client_id, request_id) -&amp;gt; Result&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;p&gt;Use a &amp;ldquo;request → result&amp;rdquo; mapping table; when an old request arrives, return the cached result.&lt;/p&gt;&#xA;&lt;p&gt;The Server cannot save history indefinitely.&lt;/p&gt;&#xA;&lt;h3 id=&#34;23--garbage-collection-under-the-single-request-assumption&#34;&gt;2.3  Garbage Collection Under the Single-Request Assumption&lt;/h3&gt;&#xA;&lt;p&gt;Client single-request assumption: the Client will not send a new command before receiving the reply to the current request.&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Client behavior:&#xA;send req=1 → wait for reply → receive&#xA;send req=2 → wait for reply → receive&#xA;send req=3 → ...&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Under this assumption:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Client sent request_id=N+1&#xA;→ means it has received replies for request_id≤N&#xA;→ Server can discard records for request_id≤N&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;So we only need to keep the last one:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;history = {&#xA;  client_A: (last_id, last_result),&#xA;  client_B: (last_id, last_result),&#xA;}&#xA;&#xA;On receiving new request N:&#xA;  history[client_id] = (N, result)&#xA;  // automatically overwrites old, keeps only latest&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;HashMap: client_id -&amp;gt; (request_id, Result)&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;h3 id=&#34;24--complete-at-most-once-logic&#34;&gt;2.4  Complete At-Most-Once Logic&lt;/h3&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Server state:&#xA;- history: map[client_id → (last_request_id, last_result)]&#xA;&#xA;On receiving Request(request_id, command) from client_id:&#xA;  &#xA;  record = history.get(client_id)&#xA;  &#xA;  if record does not exist:&#xA;      // first time seeing this Client&#xA;      result = execute(command)&#xA;      history[client_id] = (request_id, result)&#xA;      send Reply(request_id, result)&#xA;      return&#xA;  &#xA;  if request_id &amp;lt;= record.last_request_id:&#xA;      // duplicate or out-of-order old request&#xA;      send Reply(request_id, record.last_result)&#xA;      return&#xA;  &#xA;  // new request (request_id &amp;gt; last_request_id)&#xA;  result = execute(command)&#xA;  history[client_id] = (request_id, result)  // overwrite old record&#xA;  send Reply(request_id, result)&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;3--exactly-once-rpc-complete-architecture&#34;&gt;3  Exactly-Once RPC Complete Architecture&lt;/h2&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Client Node                    Server Node&#xA;┌──────────────┐              ┌────────────────┐&#xA;│ Retry Logic  │   Command    │ AMOApplication │&#xA;│    (ALO)     │  ─────────→  │     (AMO)      │&#xA;│              │    Result    │       ↓        │&#xA;│              │  ←─────────  │  Application   │&#xA;│              │              │   (business)   │&#xA;└──────────────┘              └────────────────┘&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Detailed version:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Client Node                              Server Node&#xA;┌─────────────────────────┐             ┌──────────────────────────┐&#xA;│ Retry Logic (ALO impl)  │             │ AMOApplication (AMO impl)│&#xA;│                         │             │                          │&#xA;│ • request_id: 1,2,3...  │  Command    │ • Dedup check:           │&#xA;│ • Timer: retry timeout  │  +request_id│   req_id &amp;lt;= last_req_id? │&#xA;│ • Match reply: ID match?│             │                          │&#xA;│                         │ ─────────→  │ • Save history:          │&#xA;│                         │             │   map[client_id→         │&#xA;│                         │             │       (req_id, result)]  │&#xA;│                         │  Result     │                          │&#xA;│                         │  +request_id│   ↓ if new request       │&#xA;│ Match → stop retry      │ ←─────────  │                          │&#xA;│                         │             │ Application (Business)   │&#xA;│                         │             │ • Get/Put/Append         │&#xA;│                         │             │ • Transfer/Query         │&#xA;│                         │             │ • ... (any business)     │&#xA;└─────────────────────────┘             └──────────────────────────┘&#xA;         ↑                                         ↑&#xA;client_id provided by network          Get client_id from message&#xA;framework (sender address)             (sender address)&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Server internal layering (how it works)&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Server internals:&#xA;┌─────────────────────┐&#xA;│  AMOApplication     │ ← deduplication layer&#xA;│      (wrapper)       │   only cares about request_id&#xA;│                     │   doesn&amp;#39;t care about business logic&#xA;│        ↓            │&#xA;│  Application        │ ← business layer&#xA;│   (concrete logic)   │   only cares about business logic&#xA;│  • KVStore          │   doesn&amp;#39;t care about deduplication&#xA;│  • GameServer       │&#xA;│  • ShoppingCart     │&#xA;└─────────────────────┘&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;4--extensions&#34;&gt;4  Extensions&lt;/h2&gt;&#xA;&lt;p&gt;Not all operations fear repetition&lt;/p&gt;&#xA;&lt;h3 id=&#34;41--side-effects-and-idempotence&#34;&gt;4.1  Side Effects and Idempotence&lt;/h3&gt;&#xA;&lt;p&gt;&lt;strong&gt;Side Effect = operations that change system state&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;No side effects (read operations):&#xA;- GET(key)          - read-only, doesn&amp;#39;t change state&#xA;- QUERY_BALANCE()   - read-only, doesn&amp;#39;t change state&#xA;&#xA;With side effects (write operations):&#xA;- PUT(key, value)   - modifies state&#xA;- TRANSFER(amount)  - modifies state&#xA;- APPEND(key, val)  - modifies state&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;If an operation has side effects, repeating it may produce wrong results.&lt;/p&gt;&#xA;&lt;p&gt;&lt;strong&gt;Idempotence = executing multiple times has the same effect as executing once&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Idempotent operations:&#xA;- GET(key)            - read multiple times, same result&#xA;- PUT(key, &amp;#34;value&amp;#34;)   - set to fixed value, repeat sets still that value&#xA;- DELETE(key)         - delete (deleting already deleted = still deleted)&#xA;&#xA;Non-idempotent operations:&#xA;- APPEND(key, value)  - each append makes it longer&#xA;- INCREMENT(counter)  - each adds 1, gets bigger&#xA;- TRANSFER(amount)    - each deducts money, gets smaller&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Why does it matter?&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;p&gt;If operations are idempotent, the system can be simplified:&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Client: not sure if it succeeded? Just retry&#xA;Server: repeating execution is fine&#xA;→ no need for complex AMO mechanism, at-least-once is enough&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;This is why systems like DNS, NFS can use simple at-least-once.&lt;/p&gt;&#xA;&lt;h3 id=&#34;42--scope-of-our-solution&#34;&gt;4.2  Scope of Our Solution&lt;/h3&gt;&#xA;&lt;p&gt;Applies to:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Non-idempotent operations (need AMO protection)&#xA;&lt;ul&gt;&#xA;&lt;li&gt;APPEND, INCREMENT, TRANSFER, etc.&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;Client single-request mode (garbage collection depends on this assumption)&#xA;&lt;ul&gt;&#xA;&lt;li&gt;one Client sends only one request at a time&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;Stateful services (need to remember history)&#xA;&lt;ul&gt;&#xA;&lt;li&gt;services that require persistent storage&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h2 id=&#34;5--conclusion&#34;&gt;5  Conclusion&lt;/h2&gt;&#xA;&lt;p&gt;Lab 1 looks simple: implement an RPC.&lt;/p&gt;&#xA;&lt;p&gt;But it reveals the core challenge of distributed systems: &lt;strong&gt;how do we build determinism from uncertainty?&lt;/strong&gt;&lt;/p&gt;&#xA;&lt;p&gt;Our answer is not to eliminate uncertainty (we can&amp;rsquo;t), but rather:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Acknowledge the network is unreliable&lt;/li&gt;&#xA;&lt;li&gt;Decompose the problem (exactly-once = at-least + at-most)&lt;/li&gt;&#xA;&lt;li&gt;Layered design (Client retry + Server deduplication)&lt;/li&gt;&#xA;&lt;li&gt;Leverage assumptions (single-request → garbage collection mechanism)&lt;/li&gt;&#xA;&lt;li&gt;Combine simple mechanisms into complex guarantees&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;p&gt;This is not just a Lab solution—it&amp;rsquo;s the fundamental philosophy of distributed system design.&lt;/p&gt;&#xA;&lt;p&gt;Next: Lab 2 will introduce Primary-Backup, achieving reliable service on unreliable servers.&lt;/p&gt;&#xA;</content:encoded>
      <category>Distributed_systems</category>
    </item>
  </channel>
</rss>